const {getGroupSettings,updateGroupSettings}=require('../../database');module.exports={name:'setwelcome',aliases:[],category:'group',description:'Set welcome template',usage:'.setwelcome text with @user @group',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const text=args.join(' ');if(!text)return extra.reply('⚠️ Usage: .setwelcome <message>');const s=getGroupSettings(extra.from);updateGroupSettings(extra.from,{...s,welcomeMessage:text});await extra.reply('✅ Welcome template updated.');}catch(e){await extra.reply(`❌ ${e.message}`);}}};