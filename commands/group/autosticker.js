const {getGroupSettings,updateGroupSettings}=require('../../database');module.exports={name:'autosticker',aliases:['as'],category:'group',description:'Toggle auto sticker',usage:'.autosticker on/off',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const op=(args[0]||'').toLowerCase();if(!['on','off'].includes(op))return extra.reply('⚠️ Usage: .autosticker on/off');const s=getGroupSettings(extra.from);updateGroupSettings(extra.from,{...s,autosticker:op==='on'});await extra.reply(`🎨 AutoSticker ${op==='on'?'enabled':'disabled'}.`);}catch(e){await extra.reply(`❌ ${e.message}`);}}};