const {updateChatSettings,getChatSettings}=require('../../database');module.exports={name:'mute',aliases:[],category:'group',description:'Mute chat in bot logic',usage:'.mute',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const c=getChatSettings(extra.from);updateChatSettings(extra.from,{...c,muted:true});await extra.reply('🔇 Chat muted for bot responses.');}catch(e){await extra.reply(`❌ ${e.message}`);}}};