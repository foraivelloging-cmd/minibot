const {getGroupSettings,updateGroupSettings}=require('../../database');module.exports={name:'antifake',aliases:[],category:'group',description:'Toggle antifake numbers',usage:'.antifake on/off',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const op=(args[0]||'').toLowerCase();if(!['on','off'].includes(op))return extra.reply('⚠️ Usage: .antifake on/off');const s=getGroupSettings(extra.from);updateGroupSettings(extra.from,{...s,antifake:op==='on'});await extra.reply(`🕵️ AntiFake ${op==='on'?'enabled':'disabled'}.`);}catch(e){await extra.reply(`❌ ${e.message}`);}}};