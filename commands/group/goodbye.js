const {getGroupSettings,updateGroupSettings}=require('../../database');module.exports={name:'goodbye',aliases:['gb'],category:'group',description:'Toggle goodbye messages',usage:'.goodbye on/off',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const op=(args[0]||'').toLowerCase();if(!['on','off'].includes(op))return extra.reply('⚠️ Usage: .goodbye on/off');const s=getGroupSettings(extra.from);updateGroupSettings(extra.from,{...s,goodbye:op==='on'});await extra.reply(`👋 Goodbye messages ${op==='on'?'enabled':'disabled'}.`);}catch(e){await extra.reply(`❌ ${e.message}`);}}};