const {getGroupSettings,updateGroupSettings}=require('../../database');module.exports={name:'antiviewonce',aliases:['avo'],category:'group',description:'Toggle anti view once',usage:'.antiviewonce on/off',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const op=(args[0]||'').toLowerCase();if(!['on','off'].includes(op))return extra.reply('⚠️ Usage: .antiviewonce on/off');const s=getGroupSettings(extra.from);updateGroupSettings(extra.from,{...s,antiviewonce:op==='on'});await extra.reply(`👁️ AntiViewOnce ${op==='on'?'enabled':'disabled'}.`);}catch(e){await extra.reply(`❌ ${e.message}`);}}};