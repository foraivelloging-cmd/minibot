const {getGroupSettings,updateGroupSettings}=require('../../database');module.exports={name:'antibot',aliases:[],category:'group',description:'Toggle antibot',usage:'.antibot on/off',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const op=(args[0]||'').toLowerCase();if(!['on','off'].includes(op))return extra.reply('⚠️ Usage: .antibot on/off');const s=getGroupSettings(extra.from);updateGroupSettings(extra.from,{...s,antibot:op==='on'});await extra.reply(`🤖 AntiBot ${op==='on'?'enabled':'disabled'}.`);}catch(e){await extra.reply(`❌ ${e.message}`);}}};