const {getGroupSettings,updateGroupSettings}=require('../../database');module.exports={name:'welcome',aliases:['wc'],category:'group',description:'Toggle welcome messages',usage:'.welcome on/off',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const op=(args[0]||'').toLowerCase();if(!['on','off'].includes(op))return extra.reply('⚠️ Usage: .welcome on/off');const s=getGroupSettings(extra.from);updateGroupSettings(extra.from,{...s,welcome:op==='on'});await extra.reply(`✨ Welcome messages ${op==='on'?'enabled':'disabled'}.`);}catch(e){await extra.reply(`❌ ${e.message}`);}}};