const {clearWarnings}=require('../../database');module.exports={name:'resetwarn',aliases:['clearwarn'],category:'group',description:'Clear user warnings',usage:'.resetwarn @user',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const target=msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0];if(!target)return extra.reply('⚠️ Mention user: .resetwarn @user');clearWarnings(extra.from,target);await extra.reply('✅ Warnings cleared.');}catch(e){await extra.reply(`❌ ${e.message}`);}}};