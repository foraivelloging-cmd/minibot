module.exports={name:'hidetag',aliases:['ht'],category:'group',description:'Send hidden mention message',usage:'.hidetag text',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const text=args.join(' ')||'📢';const mentions=extra.participants.map(p=>p.id);await sock.sendMessage(extra.from,{text,mentions},{quoted:msg});}catch(e){await extra.reply(`❌ ${e.message}`);}}};