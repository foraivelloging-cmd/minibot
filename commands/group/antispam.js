const {getGroupSettings,updateGroupSettings}=require('../../database');module.exports={name:'antispam',aliases:[],category:'group',description:'Toggle antispam',usage:'.antispam on/off',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const op=(args[0]||'').toLowerCase();if(!['on','off'].includes(op))return extra.reply('⚠️ Usage: .antispam on/off');const s=getGroupSettings(extra.from);updateGroupSettings(extra.from,{...s,antispam:op==='on'});await extra.reply(`🚫 AntiSpam ${op==='on'?'enabled':'disabled'}.`);}catch(e){await extra.reply(`❌ ${e.message}`);}}};