module.exports={name:'vcf',aliases:['contacts'],category:'group',description:'Export members as VCF',usage:'.vcf',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const cards=extra.participants.map((p,i)=>`BEGIN:VCARD\nVERSION:3.0\nFN:Member ${i+1}\nTEL;type=CELL;type=VOICE;waid=${String(p.id).split('@')[0]}:+${String(p.id).split('@')[0]}\nEND:VCARD`).join('\n');const buff=Buffer.from(cards);await sock.sendMessage(extra.from,{document:buff,mimetype:'text/x-vcard',fileName:'group-members.vcf',caption:'📇 Group contacts exported.'},{quoted:msg});}catch(e){await extra.reply(`❌ ${e.message}`);}}};