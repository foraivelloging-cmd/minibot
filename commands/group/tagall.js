module.exports={name:'tagall',aliases:['all'],category:'group',description:'Tag all members',usage:'.tagall message',groupOnly:true,adminOnly:true,async execute(sock,msg,args,extra){try{const text=args.join(' ')||'📣 Tagging all members';const mentions=extra.participants.map(p=>p.id);await sock.sendMessage(extra.from,{text,mentions},{quoted:msg});}catch(e){await extra.reply(`❌ ${e.message}`);}}};